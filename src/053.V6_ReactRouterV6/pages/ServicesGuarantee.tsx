import React from 'react';

export default function ServicesGuarantee() {
 return (
  <p className="bg-neutral-600 text-white">
   This is called through the Outlet, but has another path, it also call the
   Services component.
  </p>
 );
}
